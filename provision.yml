---
# Configure docker-machine host
# - import_playbook: confifure_dm_host.yml

# Provision swarm nodes
- import_playbook: provisioners/docker-machine.yml

# Configure provisioned swarm nodes.
# - import_playbook: configure.yml
